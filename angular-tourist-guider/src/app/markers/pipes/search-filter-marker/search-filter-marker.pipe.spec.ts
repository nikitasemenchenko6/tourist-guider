import { SearchFilterMarkerPipe } from './search-filter-marker.pipe';

describe('SearchFilterMarkerPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchFilterMarkerPipe();
    expect(pipe).toBeTruthy();
  });
});
